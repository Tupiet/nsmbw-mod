---
# Replaces idk what model

source_files: [../src/customTimer.cpp]
hooks:
  - name: CustomTimer
    type: add_func_pointer
    src_addr_pal: 0x809803B4
    target_func: 'daCustomTimer::build()'

  - name: CustomTimerSpriteFileInfo
    type: add_func_pointer
    src_addr_pal: 0x8031aed4
    target_func: 'TCarcNameList'
    # 0x8031AB4C + sprite num * 0x4 == offset